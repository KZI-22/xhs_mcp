# Reuse the already deployed runtime and bundled Chromium, then overlay reviewed source.
FROM ghcr.io/kzi-22/xhs_mcp:latest

ENV PYTHONPATH=/app/src \
    XHS_BROWSER_CHANNEL=chromium

COPY src /app/src
